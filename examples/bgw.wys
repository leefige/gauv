bgw = lambda parties: ps. lambda vals: W ps int.
    let shares =
        wfold(None, vals,
            // cumulator: init as None, output type;
            // party: current iterating party;
            // val: current iterating value.
            lambda cumulator. lambda party. lambda val. match cumulator with
                | None  => Some 
        )




let a   =par({p1})= read() in
let b   =par({p2})= read() in
let c   =par({p3})= read() in

let 
